- name: variables from arguments
  hosts: localhost
  tasks:  
    - name: get from arguments with passing prompt
      ansible.builtin.debug:
        msg: " hello myself {{person}}, how are you doing {{do}}" 